name: Generate Book Report

on:
  schedule:
    - cron: '0 14 * * 3'  # Wednesday 2pm UTC (weekly)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Generate book report
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/generate_book_report.py

      - name: Notify IndexNow
        if: success()
        run: |
          NEW_FILE=$(git diff --name-only HEAD~1 HEAD -- 'src/content/' | head -1)
          if [ -n "$NEW_FILE" ]; then
            SLUG=$(basename "$NEW_FILE" .md)
            curl -s -X POST "https://api.indexnow.org/indexnow" \
              -H "Content-Type: application/json; charset=utf-8" \
              -d "{\"host\":\"litrpgcritic.com\",\"key\":\"lrc8f3a1d7e2b9c4\",\"urlList\":[\"https://litrpgcritic.com/blog/${SLUG}/\",\"https://litrpgcritic.com/blog/\",\"https://litrpgcritic.com/sitemap-index.xml\"]}" || true
          fi
